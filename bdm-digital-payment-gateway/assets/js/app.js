document.addEventListener("DOMContentLoaded", function () {
    const root = document.getElementById("bdm-checkout-root");
    if (root) {
        root.innerHTML = "<h1>Checkout Page Running React</h1>";
    }
});